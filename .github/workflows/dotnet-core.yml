name: Build, Test & Generate Docs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-test-generate-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup .NET 8.0
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 8.0.x

      - name: Setup Doxygen
        run: sudo apt-get install -y doxygen

      - name: Installing dependencies...
        run: dotnet restore

      - name: Building...
        run: dotnet build

      - name: Running test...
        run: dotnet test

      - name: Generating docs...
        working-directory: docs
        run: doxygen Doxyfile
        continue-on-error: false

      - name: Commit and push generated docs
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          dotnet build    # Modifica el Library.xml
          git stash --include-untracked
          git fetch origin gh-pages || true
          git checkout gh-pages

          # Limpiar todo y copiar los nuevos docs
          rm -rf docs/html/*
          git stash pop || (
            git checkout --theirs . &&
            git add . &&
            echo "Conflicts resolved in favor of stash"
          )

          # Commit y push
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Update Doxygen docs [skip ci]" || echo "No changes to commit"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }} gh-pages

